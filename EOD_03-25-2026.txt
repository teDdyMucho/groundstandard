Angelo Tapang  25/03/2026
EOD: GroundStandard Image Content Editor — Folder System & Performance

1. Fixed button nesting DOM warning — brand profile picker card was a button containing edit and delete buttons inside it, changed outer element to a div with role button to fix the validateDOMNesting error

2. Moved Create Folder button to the header bar next to Brand Profile and refresh, opens as a proper centered modal instead of inline input in the gallery

3. Built full folder picker modal — larger layout, shows folder count and total images, each folder displays image count and creation date, includes rename inline editing with save, delete with trash icon, and new folder creation at the bottom

4. Added folder rename functionality — pencil icon on each folder in the modal, click to edit name inline, saves to database and updates local state

5. Added folder filter across all three tabs — Gallery, Review Queue, and Schedule all respect the selected folder, only showing images that belong to that folder

6. Built folder selection landing screen — on first load or refresh, all three tabs show a folder selection view before displaying any content, user must pick a folder first

7. Added Unfiled option in folder landing — if there are images not assigned to any folder they show as an Unfiled option with dashed border styling

8. Added informative helper text on each tab landing — Gallery says you can still upload images and they go to unfiled, Review Queue says to generate content first, Schedule says to export from review queue

9. Added active folder indicator bar on Review Queue and Schedule tabs — shows which folder is currently selected with a Change Folder button to go back to folder selection

10. Added empty state pages for all tabs — when a folder or unfiled has no content it shows a proper message like "No images in Unfiled" or "No posts in Karate" with action buttons

11. Added bulk move to folder — Select All button in gallery selects all images regardless of status, Move to Folder dropdown appears when images are selected, moves all selected images to chosen folder in one database call

12. Made gallery checkboxes visible on all images — previously only uploaded status images had checkboxes, now all images can be selected for bulk operations like move to folder

13. Fixed Supabase Realtime re-render storm — added suppressRealtimeRef that tracks IDs being updated locally so when bulk move or bulk approve triggers hundreds of realtime events they get skipped instead of causing hundreds of re-renders

14. Fixed ERR_INSUFFICIENT_RESOURCES browser error — lucide-react was excluded from Vite dependency optimization causing hundreds of individual HTTP requests for each icon file, changed from exclude to include so Vite bundles them properly

15. Reverted thumbnail URL back to original format — removed quality and webp format params that were added during debugging, kept the original resize contain that was working fine before

16. Added folder-aware tab badges — Review Queue and Schedule tab badges now only count posts belonging to the currently selected folder instead of counting everything

17. Fixed Review Queue card rendering not respecting folder filter — the reviewFiltered variable used for actual card display was missing the folder filter, added the same folder filter logic so cards match the selected folder
